# KV-BM / TRT-LLM extra options for Llama 3.1 8B (Stack B Dynamo)
kv_cache_config:
  dtype: "auto"
  free_gpu_memory_fraction: 0.80
cuda_graph_config:
  enable_padding: true
disable_overlap_scheduler: true
